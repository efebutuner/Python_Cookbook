# Dictionary keys could be numbers, boolean values but they cannot be unhashable types like a list. 
# Keys should be immutable
# If a key in a dictionary is double or not single, it overrites and prints the last value its assigned inside the dictionary.
