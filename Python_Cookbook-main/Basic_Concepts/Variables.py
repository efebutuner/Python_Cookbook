iq = 190
print(iq)

#Variables are usually named with snake_case, which means they should be starting with lowercases or underscores
#Variables are also case sensitive which means we should watch out the uppercase or lowercase characters to be matched while recalling our variables

user_iq = 190
user_age = user_iq / 4
print(user_age)

# constants: variables that are assigned using all Upercase characters that means this value should not be changed within the code.
PI = 3.14

a,b,c = 1, 2, 3 # We can make rapid assignments to variables using commas.